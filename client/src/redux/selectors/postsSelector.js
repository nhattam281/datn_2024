export const postsSelector = (state) => state.posts.posts;
export const postsSavedSelector = (state) => state.posts.postsSaved;
export const myPostsSelector = (state) => state.posts.myPosts;
export const postsRecommendSelector = (state) => state.posts.postRecommend;
